// =============================================================================
//  trt_alpha :: cls :: types
// -----------------------------------------------------------------------------
//  ClassScore —— 一个类别得分（分类任务的 top-k 一项）。
// =============================================================================
#pragma once

namespace trt_alpha::cls {

//! 一个类别得分。
struct ClassScore
{
    int label = -1;       //!< 类别 ID
    float score = 0.f;    //!< 得分（通常是 softmax 概率）
};

}  // namespace trt_alpha::cls